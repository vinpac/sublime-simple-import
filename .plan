1. Takes an expression and its context
  - fields
    - react
    - import react
2. Find the interpreter that matchs the file type
3. Interprete the the expression with its context:
  - Return
    - Interpreted
      - interpreter
      - itype
      - statements
      - insert_type = 'insert'
      - Import import

  - Import
    - expression
    - context
    - expression_region
    - context_region
5. Ask the interpreter what to search for
  - Arguments
    - interpreted
  - Return
    - String
      - query
    - Dict
      - file
      - module
      - exporting
    - Boolean
      # Either to search or not
6. Fire on Interpreter onAfterSearchResult
  - Arguments
    - interpreted
    - result
    - result_type
  - Return
    - interpreted
7. Ask the interpreter if its necessary to search for similar imports whithin the view
  - Arguments
    - interpreted
  - Return
    - Boolean
7.1. Ask the interpreter to resolve the Interpreted with the similar imports
    - Arguments
    - interpreted
  - Return
    - interpreted

0. Types of insertion
  - INSERT
  - INSERT_AFTER
  - INSERT_BEFORE
  - REPLACE
  - REPLACE_IMPORT
